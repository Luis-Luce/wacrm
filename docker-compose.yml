version: '3.8'
services:
  wacrm:
    build: .
    container_name: wacrm_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    restart: unless-stopped
